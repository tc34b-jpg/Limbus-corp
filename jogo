<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Intro do ManoBit</title>
    <style>
        /* Reset básico pra não dar ruim */
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #121212; /* Cor de fundo de hacker */
            color: white;
            overflow: hidden; /* Evita scroll enquanto a intro tá ativa */
        }

        /* A TELA DE INTRO */
        #intro-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: linear-gradient(135deg, #1e1e2f 0%, #111 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.8s ease, visibility 0.8s;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 0 0 10px #00ff88;
        }

        /* O BOTÃO ESTILOSO */
        .btn-start {
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: bold;
            color: #121212;
            background-color: #00ff88;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0, 255, 136, 0.4);
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-start:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 20px rgba(0, 255, 136, 0.6);
        }

        /* Classe que o JS vai colocar pra sumir com tudo */
        .hidden {
            opacity: 0;
            visibility: hidden;
        }

        /* Conteúdo que aparece DEPOIS */
        #main-content {
            padding: 50px;
            text-align: center;
        }
    </style>
</head>
<body>

    <div id="intro-screen">
        <h1>BEM-VINDO AO FUTURO ⚡</h1>
        <button class="btn-start" onclick="entrar()">ENTRAR NO SISTEMA</button>
    </div>

    <div id="main-content">
        <h2>Boa! Você entrou. 😎</h2>
        <p>Agora o site de verdade começa aqui...</p>
    </div>

    <script>
        function entrar() {
            const intro = document.getElementById('intro-screen');
            // Dá o sumiço na tela de intro
            intro.classList.add('hidden');
            
            // Libera o scroll depois que a intro sumir
            setTimeout(() => {
                document.body.style.overflow = 'auto';
            }, 800);
        }
    </script>

</body>
</html>
